# VPS IP: 159.223.63.117
# Production Domain Configuration with HTTPS

# Main domain with automatic HTTPS
aibreeds-demo.com {
  reverse_proxy app:3000
}

# IP address access (HTTP only)
http://159.223.63.117 {
  reverse_proxy app:3000
}
